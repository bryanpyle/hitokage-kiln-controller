setuptools<81  # packaging/build utilities; <81 keeps pkg_resources for blinka compatibility
greenlet  # lightweight coroutines used by gevent
bottle  # lightweight web framework for HTTP routes (legacy; kept for kiln-logger/watcher scripts)
gevent  # async WSGI server/event loop (legacy)
gevent-websocket  # websocket handler integration for gevent (legacy)
websocket-client  # client websocket support (used by logger scripts)
requests  # HTTP client for watcher/utility scripts

# ── FastAPI backend (new) ─────────────────────────────────────────────────────
fastapi>=0.115.0          # Modern async web framework
uvicorn[standard]>=0.32.0 # ASGI server with uvloop + websockets
pydantic>=2.9.0           # Data validation (bundled with FastAPI)

# for folks running raspberry pis
# we have no proof of anyone using another board yet, but when that 
# happens, you might want to comment this out.
RPi.GPIO  # Raspberry Pi GPIO pin control (hardware only)

# List of all supported adafruit modules for thermocouples
adafruit-circuitpython-max31855  # MAX31855 thermocouple interface
adafruit-circuitpython-max31856  # MAX31856 thermocouple interface

# for folks using sw spi (bit banging)
adafruit-circuitpython-bitbangio  # software SPI support for compatible boards

# untested - for PT100 platinum thermocouples
#adafruit-circuitpython-max31865  # optional PT100/RTD amplifier support

# untested - for mcp9600 and mcp9601
#adafruit-circuitpython-mcp9600  # optional MCP9600/9601 thermocouple support
